{
  "name": "odia-english-translator-server",
  "version": "2.0.0",
  "description": "Backend for a cascade speech pipeline using only free services: browser ASR, free MyMemory translation, browser TTS.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
